import { expect } from 'chai';

describe("True", () => {
    describe("True", () => {
        it("True", () => {
            let result = true;
            expect(result).to.equal(true);
        });
    })
});
